# FormShift 当前能力与开发路线

更新日期：2026-08-16  
当前阶段：`0.3.0-dev` PDF 工作台完备版

这份文档只记录真实能力和明确边界。这里的“可用”必须至少已经接入应用界面和真实转换引擎；“已验证”表示存在实际转换、结果回读或自动化测试证据。格式名称出现在代码或界面中，不等于对应编码器已经可用。

## 一句话结论

FormShift 当前是一款可实际使用的本地图片、音视频格式转换及 PDF 综合工作台开发版。批量队列、多目标输出、多图合成多页 PDF、PDF 批量提取图片、PDF 合并/拆分/重排/旋转、历史记录和预设均已完整实现并经过本地验证。

## 当前可以做什么

### 图片

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 读取 JPEG、PNG、HEIC、TIFF、BMP、WebP、AVIF | 可用，运行时检测 | 只接受 macOS ImageIO 实际能够解码的格式，不只相信文件扩展名。 |
| 输出 JPEG、PNG、HEIC、TIFF、BMP、WebP、AVIF、GIF | 条件可用 | 只显示当前 Mac 的 ImageIO 实际提供编码器的格式；某台 Mac 没有 WebP 或 AVIF 编码器时不会显示为可输出。 |
| 图片质量调整 | 可用 | 对支持有损质量参数的输出生效。 |
| 修改宽高 | 可用 | 支持完整适应、填充裁切和拉伸。 |
| 智能裁剪白边 | 可用、已验证 | 已通过无需 XCTest 的真实冒烟测试。 |
| 手动裁剪、90°/180°/270° 旋转 | 可用 | 裁剪范围会在执行前验证。 |
| 保留或移除元数据 | 可用 | 由目标格式和系统编码器能力决定最终可保留字段。 |
| sRGB、Display P3 | 可用、已验证 | Display P3 输出已进行结果回读验证。 |
| 同一图片一次输出多个格式 | 可用、已验证 | 每种目标格式有独立任务、设置、结果和历史记录。 |

说明：动态 GIF 不会交给静态图片引擎处理，以免悄悄丢失动画并只留下第一帧。

### PDF

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 多图/单图合成多页 PDF | 可用、已验证 | 支持多张图片按指定顺序、版式（适应尺寸、A4 纵横、Letter 纵横）和边距合成单份多页 PDF。 |
| PDF 批量导出图片 | 可用、已验证 | 支持导出全部页面、仅第一页或自定义页码区间（如 1-3, 5）为 PNG、JPEG、TIFF。 |
| PDF 合并 | 可用、已验证 | 支持将多个 PDF 文件及混合图片按顺序合并为单份 PDF。 |
| PDF 拆分 | 可用、已验证 | 支持单页独立拆分、固定页数均分（每 N 页一份）或指定页码区间拆分。 |
| PDF 页面重排与旋转 | 可用、已验证 | 视觉网格缩略图预览，支持页面顺序左移右移/反转、单页或全部顺/逆时针 90° 旋转、单页剔除。 |
| PDF 渲染精度 1×、2×、3× | 可用、已验证 | 1× 与 3× 输出尺寸差异已自动验证。 |
| 导出时缩放、旋转、智能裁白边 | 可用 | PDF 首页暂不支持手动裁剪。 |

### 视频、音频与 GIF

只有应用找到内置 FFmpeg/ffprobe 时，这部分能力才会出现。开发构建已经验证优先使用包内 FFmpeg，不依赖 Homebrew 或用户的 `PATH`。

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 读取 MP4、MOV、MKV、WebM | 可用 | 使用 ffprobe 检查真实容器和流信息。 |
| 输出 MP4、MOV、MKV | 可用、已验证 | 当前固定构建使用实际存在的 H.264、HEVC 或 ProRes 编码器。 |
| 视频转 GIF | 可用、已验证 | 支持尺寸、裁切、旋转、帧率和时间裁切的引擎参数。 |
| GIF 转 MP4、MOV、MKV | 可用 | 只显示当前编码器能够完成的目标格式。 |
| GIF 重新编码为 GIF | 可用 | 不走会丢动画的 ImageIO 静态路径。 |
| 视频提取音频 | 可用、已验证 | 已实际验证 M4A、AAC、WAV、AIFF、FLAC、ALAC、OGG、Opus。 |
| 音频格式互转 | 可用 | 输入支持 MP3、M4A/AAC、WAV、AIFF、FLAC、ALAC、OGG、Opus；输出只显示当前 FFmpeg 实际具备编码器的格式。 |
| 视频/音频时间裁切、码率、静音、响度标准化 | 引擎已实现，界面未完整开放 | 当前代码具备参数能力，但还不能把全部选项都算作普通用户可直接操作的完整功能。 |

### 批处理、队列与安全

- 支持拖入文件和文件夹，也支持一次选择多个文件。
- 一个源文件可同时生成多个目标格式，每种格式独立保存质量、尺寸和专业参数。
- 支持队列开始、暂停新任务、取消、失败重试和中断恢复。
- 支持最近 30 天任务摘要、点击历史记录恢复上次设置并再次转换、打开结果和在 Finder 中定位。
- 支持创建、应用、同名更新、删除和跨重启保存预设。
- 默认输出到原文件目录，也可使用固定目录或每次询问。
- 永不覆盖源文件；重名自动增加序号。
- 先写任务专属临时文件，成功后再移动到最终位置；失败或取消会清理临时文件。
- 完全本地运行，无账号、无上传、无广告、无遥测，转换引擎不会静默下载组件。

## 当前不能做什么

| 当前不可用 | 真实原因 | 计划 |
| --- | --- | --- |
| GIF 拆帧 | 还没有多结果文件夹输出模型和帧命名规则 | `0.4` |
| 图片序列生成 GIF | 还没有序列排序、帧时长和画布统一流程 | `0.4` |
| 视频按时间或间隔导出帧 | 还没有多文件结果管理和对应界面 | `0.4` |
| 预设导入、导出、批量编辑 | 当前预设只保存在本机应用数据中 | `0.4` |
| MP3 输出 | 固定 FFmpeg 构建未内置可发布的 MP3 编码器 | `0.5` |
| WebM 输出、VP9 编码 | 固定构建未内置 libvpx/对应编码器 | `0.5` |
| AV1 输出 | 固定构建未内置经过性能与许可验收的 AV1 编码器 | `0.6` |
| 完整的视频码率、音频码率、裁切时间、静音和响度界面 | 引擎有能力，但界面和参数校验尚未完整交付 | `0.4` |
| Developer ID 签名、Apple 公证的正式 DMG | 需要有效的 Apple Developer ID、完整发布配置和干净 Mac 验收 | `1.0`，取决于证书 |
| Office 文档转换 | 没有 Office 文档排版引擎；macOS 原生框架不能可靠完成 Word/Excel/PowerPoint 转换 | `2.x` 候选，不属于首版 |
| OCR、扫描 PDF 转可编辑 Word | 这不是普通格式改封装，需要 OCR、版面分析和文档重建 | 未排期 |
| PDF 直接转可编辑 Word | 需要独立的版面重建引擎，无法承诺原排版无损 | 未排期 |
| 电子书、压缩包、AI 功能 | 不在当前产品范围 | 未排期 |

## 什么时候可以做

以下是从 2026-08-15 开始、开发工作连续进行时的工程估算，不是已经发布的承诺。任何阶段只有在真实样本转换、结果回读、异常测试和 UI 验收通过后才会标记为“可用”。

| 阶段 | 目标 | 预计开发周期 | 最早目标窗口 |
| --- | --- | --- | --- |
| `0.3` PDF 工作台 | 合并、拆分、页面旋转、重排、多页图片导出、多图合成 PDF | 7–10 个开发日 | 2026-08-25 至 2026-08-29 |
| `0.4` 帧与工作流 | GIF 拆帧、图片序列转 GIF、视频导出帧、完整媒体参数界面、预设导入导出 | 8–12 个开发日 | 2026-09-08 至 2026-09-14 |
| `0.5` 常用编码包 | MP3 输出、VP9 与 WebM 输出、固定依赖和许可证清单 | 7–12 个开发日 | 2026-09-18 至 2026-09-28 |
| `0.6` AV1 | AV1 软件/硬件路径、速度与质量预设、兼容性测试 | 5–10 个开发日 | 2026-10-05 至 2026-10-12 |
| `0.9` 发布候选 | 完整 Xcode 测试、异常素材矩阵、性能、无障碍、干净 Mac 安装验收 | 7–12 个开发日 | 2026-10-20 至 2026-10-30 |
| `1.0` 正式版 | Developer ID 签名、公证 DMG、发布说明和最终许可证包 | 获得证书后 2–4 个开发日 | 没有证书时无法给出真实日期 |
| `2.x` Office 扩展候选 | DOC/DOCX、XLS/XLSX、PPT/PPTX 等转 PDF，优先评估内置 LibreOffice | 15–30 个开发日，且会显著增大安装包 | 最早在 `1.0` 稳定后评估，当前不承诺日期 |

这些窗口按顺序估算；如果并行开发，需要先锁定共享类型和多文件输出协议，否则表面上更快，实际更容易引入数据丢失和任务冲突。

## Office 文档的明确边界

Office 文档不是“加几个扩展名”就能完成。Word、Excel 和 PowerPoint 都需要完整的字体、分页、图表、公式、动画和排版解释器。

最可行的方案是把 LibreOffice 做成可选的本地扩展，并固定版本、公开源码和许可证信息：

- 优点：仍然可以完全本地运行，常见 Office → PDF 转换的覆盖率较高。
- 代价：安装包会增加数百 MB，构建、Sandbox、签名、公证和字体兼容测试都会明显变复杂。
- 无法承诺：复杂公式、宏、嵌入对象、特殊字体和 PowerPoint 动画百分之百还原。
- 不等同于：PDF → Word、扫描件 OCR 或无损恢复可编辑排版。这三项需要另外的引擎。

因此，Office 只会作为 `2.x` 的可选扩展评估，不会为了看起来“功能全面”而在当前版本里显示一个实际不可用的入口。

## 验收规则

一项能力只有同时满足以下条件，才会从“计划中”移动到“当前可用”：

1. 界面存在可操作入口，不需要用户手工修改参数或运行命令。
2. 干净的 Apple Silicon Mac 不安装 Homebrew 也能运行。
3. 至少有正常、损坏、错误扩展名、Unicode 文件名、取消、重名和权限拒绝样本。
4. 输出能够被独立工具重新读取，并核对尺寸、页数、时长、流信息或帧数。
5. 失败和取消不会留下伪装成完成结果的残缺文件。
6. 对应能力进入自动化测试；没有成功样本的格式不能显示为“支持”。
7. 依赖的源码版本、构建参数和许可证已经固定并可复现。

## 当前测试证据边界

- Debug 与 Release 已通过 `swift build`。
- `FormShiftSmoke` 已真实验证图片裁白边、填充裁切、Display P3、PDF 1×/3×、一图多格式、历史参数恢复和中断迁移。
- 开发机已实际转换并回读 PNG → JPEG、图片 → PDF、PDF 首页 → PNG。
- 固定 FFmpeg 构建已通过真实队列验证 MP4 → MP4/MOV/MKV/GIF/M4A/AAC/WAV/AIFF/FLAC/ALAC/OGG/Opus。
- GitHub Actions 的 macOS 15 环境已经运行 XCTest；本地机器只有 Command Line Tools，不能把本地 `swift test` 说成已通过。
- 当前 DMG 是 ad-hoc 签名开发构建，不是 Developer ID 签名或 Apple 公证的正式发行版。

当代码、测试或发布状态发生变化时，应先更新本文件，再修改 README 中的功能声明。
