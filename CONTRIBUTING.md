# 贡献指南

感谢参与 FormShift。当前项目处于早期阶段，合并标准以正确性、文件安全、隐私和可验证性优先。

## 开始前

- 使用 Apple Silicon Mac、macOS 15+ 和完整 Xcode。
- 新功能先在 issue 中说明输入/输出格式、真实使用场景、依赖和验收样本。
- 不提交用户文件、版权不明的媒体、FFmpeg 二进制、签名证书、描述文件或公证凭据。
- 不引入账号、云上传、遥测或自动下载行为，除非先通过公开设计审查并更新隐私说明。

## 代码边界

- 公共模型和协议放在 `FormShiftCore`；具体转换逻辑放在对应引擎模块；界面不直接拼接 FFmpeg 命令。
- 外部进程必须使用参数数组，禁止把文件路径拼成 shell 命令。
- 输出先写任务专属临时文件，验证成功后再移动到最终位置。
- 永不覆盖源文件；失败和取消路径必须有测试并清理残留。
- UI 不能把“界面列出格式”当成“引擎已支持”。只有真实样本测试通过的组合才能对外展示为支持。
- Swift 6 并发检查必须保持通过；共享可变状态使用 actor 或明确隔离。

## 提交前检查

```sh
swift build
swift test
Scripts/check-repository.sh
```

完整 App 的拖放、沙盒、VoiceOver、减少动态效果和真实媒体转换必须在 Xcode/真机环境验证。Command Line Tools 的通过结果不能替代这些验收。

## 测试要求

- 修复缺陷时增加能复现该缺陷的测试。
- 新增格式或编解码器时提交来源清楚、体积小的测试素材及许可记录，覆盖读取、转换、输出探测和取消。
- 不把扩展名当作唯一格式证据；测试错误扩展名、损坏文件、Unicode 文件名、权限拒绝、磁盘空间不足和无音轨媒体。
- 测试不得依赖网络、用户 Homebrew 环境或未声明的系统工具。

## Pull Request

PR 描述应列出：行为变化、测试证据、隐私/许可影响、用户文件风险和未验证事项。截图只能证明界面状态，不能证明转换结果正确。发布相关 PR 还必须附带第三方组件 manifest、签名验证和公证验证输出；敏感值必须删去。

提交贡献即表示你有权按仓库的 GPLv3 许可证提供该贡献。
