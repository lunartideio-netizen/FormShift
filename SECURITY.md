# Security Policy

## 支持范围

FormShift 尚未发布稳定版本。目前只对默认分支的最新代码接受安全报告；开发构建不承诺长期补丁支持。

## 私密报告

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密提交。仓库地址尚未配置时，请暂缓公开漏洞细节，并由维护者在发布前补充安全联系渠道：`【待补充】`。

报告请包含受影响版本或 commit、复现步骤、预期与实际行为，以及最小化的无敏感信息样本。不要上传私密媒体、真实用户路径、签名证书、公证凭据或其他密钥。

## 重点风险

- 恶意或损坏媒体触发解析器/FFmpeg 漏洞。
- 文件名、路径或元数据造成参数注入。
- 符号链接、竞态条件或重名逻辑导致覆盖源文件/目标文件。
- 取消或失败后残留可被误认为成功结果的文件。
- 沙盒权限过宽、书签泄露或日志记录敏感路径。
- 被替换的 FFmpeg/辅助工具、错误签名或不可信更新包。

## 响应原则

维护者会确认报告并评估影响，但当前没有可承诺的响应时限。修复完成前可能临时移除受影响格式或停止分发。任何“已修复”“已签名”“已公证”结论都必须附可复核证据。
